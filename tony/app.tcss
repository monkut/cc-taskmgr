Screen {
    background: $surface;
}

/* In-progress bar */
InProgressBar {
    height: auto;
    dock: top;
    background: $boost;
    border-bottom: solid $accent;
}

#in-progress-section {
    height: auto;
    width: 100%;
}

#in-progress-summary {
    height: auto;
    width: 100%;
    padding: 0 1;
}

#in-progress-detail {
    padding: 0 2;
    margin: 0 0 1 0;
}

/* Filter bar - prominent row at top */
FilterBar {
    height: auto;
    max-height: 5;
    dock: top;
    background: $boost;
    border-bottom: solid $primary;
}

#filter-bar {
    height: auto;
    width: 100%;
    padding: 0 1;
}

.filter-label {
    width: auto;
    height: 1;
    text-style: bold;
    color: $accent;
    margin: 1 0 0 0;
}

#org-select {
    width: 30%;
}

#project-select {
    width: 1fr;
}

/* Issue table */
IssueTable {
    height: 1fr;
}

/* No focus border — avoids layout distortion in small terminals */

/* Status bar */
#status-bar {
    dock: bottom;
    height: 1;
    padding: 0 1;
    background: $primary-background;
    color: $text;
}

/* Issue detail view */
IssueDetail {
    height: 1fr;
}

#back-btn {
    width: auto;
    min-width: 10;
}

#issue-header {
    padding: 1;
    background: $primary-background;
    margin-bottom: 1;
}

#detail-scroll {
    height: 1fr;
    margin-bottom: 1;
}

#issue-body {
    padding: 0 1;
}

#comments-section {
    padding: 0 1;
    margin-top: 1;
}

#comment-form {
    dock: bottom;
    height: auto;
    max-height: 10;
    padding: 0 1;
}

#comment-input {
    height: 4;
    margin-bottom: 1;
}

#submit-comment {
    width: auto;
    min-width: 16;
}

/* Settings modal */
#settings-dialog {
    width: 50;
    height: auto;
    padding: 1 2;
    background: $surface;
    border: thick $primary;
    align: center middle;
}

SettingsScreen {
    align: center middle;
}

#settings-title {
    text-style: bold;
    padding-bottom: 1;
    text-align: center;
}

#settings-dialog Label {
    padding: 1 0 0 0;
}

#settings-dialog Input {
    margin: 1 0;
}

#settings-dialog Button {
    width: 100%;
    margin-top: 1;
}

/* Detail toolbar */
#detail-toolbar {
    dock: top;
    height: auto;
    width: 100%;
    padding: 0;
    margin: 0 0 1 0;
}

#detail-toolbar Button {
    width: auto;
    min-width: 10;
    margin-right: 1;
}

#action-status {
    width: 1fr;
    height: 1;
    margin: 1 0 0 1;
}

#execute-action-btn {
    min-width: 18;
}

/* Action select modal */
ActionSelectScreen {
    align: center middle;
}

#action-select-dialog {
    width: 40;
    height: auto;
    padding: 1 2;
    background: $surface;
    border: thick $primary;
}

#action-select-title {
    text-style: bold;
    padding-bottom: 1;
    text-align: center;
}

#action-select-dialog Button {
    width: 100%;
    margin-top: 1;
}

/* Confirm action modal */
ConfirmActionScreen {
    align: center middle;
}

#confirm-action-dialog {
    width: 50;
    height: auto;
    padding: 1 2;
    background: $surface;
    border: thick $primary;
}

#confirm-action-title {
    text-style: bold;
    padding-bottom: 1;
    text-align: center;
}

#confirm-action-message {
    padding: 1 0;
    text-align: center;
}

#confirm-action-dialog Button {
    width: 100%;
    margin-top: 1;
}

#loading-indicator {
    width: 100%;
    height: 100%;
    content-align: center middle;
}

/* Project dirs input in settings */
#project-dirs-input {
    height: 4;
    margin: 1 0;
}
